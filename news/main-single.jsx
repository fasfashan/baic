import React from "react";
import ReactDOM from "react-dom/client";
import SingleNews from "./single-news.jsx";
import "../src/index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SingleNews />
  </React.StrictMode>
);
